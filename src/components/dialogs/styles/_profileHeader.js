import { makeStyles } from "@material-ui/core";

const useStyles = makeStyles(theme => ({
    headerContainer: {
        marginTop: "50px",
        width: "100%",
        height: "200px",
    },
    headerImage: {
        height: "100%",
        width: "100%"
    }
}));

export default useStyles;