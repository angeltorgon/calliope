import { makeStyles } from "@material-ui/core";

const useStyles = makeStyles(theme => ({
    headerContainer: {
        marginTop: "50px",
        width: "100%",
        height: "200px",
        overflow: "hidden"
    },
    headerImage: {
        widdth: "100%"
    }
}));

export default useStyles;