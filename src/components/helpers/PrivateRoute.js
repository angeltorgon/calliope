import React from "react";

const PrivateRoute = ({ component: Commponent, ...props }) => {

    console.log("component", Component));

    return (
        <div>
            <h1> Hello! </h1>
        </div>
    );
};
